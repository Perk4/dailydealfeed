# Temp files
temp/
clips/temp/

# Node modules
node_modules/

# Environment files
.env
.env.local

# OS files
.DS_Store

# Large source videos (100MB+ full compilation videos)
clips/raw/*.webm*
clips/raw/*Fails of the Year*
clips/raw/*Gravity WINS*
clips/raw/*From Bad to WORSE*

# Analysis frames (used for moment detection, not needed in repo)
clips/frames/

# Test clips (not needed for production)
test-clips/

# Large generated videos (keep in output but track selectively)
# output/*.mp4
# output/*.jpg
